#include <stdlib.h>
#include <stdio.h>
#include "set.h"

int main(){
    Set SETA = {0};
    Set SETB = {0};
    Set SETC = {0};
    Set SETD = {0};
    Set SETE = {0};
    Set SETF = {0};

    return 0;
}
